
// src/api/auth/auth.v1.routes.ts

import { Router } from 'express';

const router = Router();

// Define your authentication routes here
// For example: router.post('/register', ...);

export default router;