
// src/api/devices/devices.v1.routes.ts

import { Router } from 'express';

const router = Router();

// Define your device routes here
// For example: router.get('/:id', ...);

export default router;